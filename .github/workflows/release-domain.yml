name: Release — MicroKit.Domain

on:
  push:
    tags:
      - 'domain-v*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore
        run: dotnet restore modules/MicroKit.Domain/MicroKit.Domain.slnx

      - name: Build
        run: dotnet build modules/MicroKit.Domain/MicroKit.Domain.slnx --no-restore -c Release

      - name: Test
        run: dotnet test modules/MicroKit.Domain/MicroKit.Domain.slnx --no-build -c Release

      - name: Pack
        run: |
          dotnet pack modules/MicroKit.Domain/MicroKit.Domain.slnx \
            --no-build -c Release \
            -o nupkgs

      - name: Push to NuGet.org
        run: |
          dotnet nuget push nupkgs/*.nupkg \
            --api-key ${{ secrets.NUGET_API_KEY }} \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate
